{
  "name": "physim2d",
  "version": "1.0.0",
  "description": "PhySim2D — Simulatore 2D di forze e vettori per studenti di prima superiore",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"sleep 2 && electron .\"",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "vite build && electron-builder --win"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0",
    "jsdom": "^26.1.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "build": {
    "appId": "com.physim2d",
    "productName": "PhySim2D",
    "win": {
      "target": "nsis"
    },
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*"
    ]
  }
}
